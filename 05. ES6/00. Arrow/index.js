const greet = (name) => {
    console.log(`Hello, ${name}!`);
};

greet("Rahul");

// ✅ What it does:
// Uses an arrow function (const greet = (name) => {...})

// Takes name as a parameter

// Logs a greeting message using template literals (backticks and ${})